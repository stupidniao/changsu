{
  "name": "changsu",
  "version": "0.1.0",
  "private": true,
  "description": "Changsu personal assistant monorepo",
  "scripts": {
    "build:miniapp": "pnpm --filter @changsu/miniapp build",
    "dev:miniapp": "pnpm --filter @changsu/miniapp dev"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}